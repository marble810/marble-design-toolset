<div class="noise-texture-creater-preview">
	<div class="noise-texture-creater-preview__badge">Preview Starter</div>
	<h2 class="noise-texture-creater-preview__title">Noise Texture Creater</h2>
	<p class="noise-texture-creater-preview__description">Replace this placeholder with your tool preview.</p>
</div>

<style>
	.noise-texture-creater-preview {
		display: flex;
		flex-direction: column;
		justify-content: flex-end;
		width: 100%;
		height: 100%;
		padding: 32px;
		background: #1a2130;
		border: 2px solid rgba(255, 255, 255, 0.16);
		box-shadow: inset 0 0 0 2px rgba(255, 255, 255, 0.04);
	}

	.noise-texture-creater-preview__badge {
		display: inline-flex;
		align-self: flex-start;
		align-items: center;
		height: 22px;
		padding: 0 var(--space-2);
		border: 1px solid rgba(255, 255, 255, 0.14);
		background: rgba(9, 13, 22, 0.72);
		color: var(--color-fg-secondary);
		font-size: var(--font-size-1);
		text-transform: uppercase;
		letter-spacing: 0.08em;
	}

	.noise-texture-creater-preview__title {
		margin: var(--space-4) 0 var(--space-2);
		font-size: 42px;
		line-height: 1;
	}

	.noise-texture-creater-preview__description {
		margin: 0;
		max-width: 360px;
		color: rgba(255, 255, 255, 0.84);
		font-size: var(--font-size-3);
	}
</style>
