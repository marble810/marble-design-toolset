## 1. 工作区会话壳层

- [ ] 1.1 引入按 `toolId` 管理的工具会话 wrapper，为每个已打开标签维护独立的定义加载状态、组件实例和壳层上下文。
- [ ] 1.2 重构 [src/routes/+page.svelte](src/routes/+page.svelte) 的单活动组件渲染路径，改为稳定 keyed 的会话栈渲染，并保持 `openToolIds`、`activeToolId` 与左侧面板宽度语义不变。
- [ ] 1.3 调整 About、MainInfo、Export Section 等 framework-owned 区块的数据来源，确保它们始终反映当前活动会话，而关闭标签会真正卸载对应会话。

## 2. 生命周期与资源管理

- [ ] 2.1 新增工具会话活动/隐藏运行时接口，并在标签切换时向每个已挂载会话分发活动状态而不触发卸载。
- [ ] 2.2 让至少一个重型或代表性工具消费该生命周期接口，验证隐藏标签可以暂停后台资源，重新激活后可以恢复。
- [ ] 2.3 验证 PreviewCanvas、导出注册和会话重新显示路径在保活模式下仍能正确工作。

## 3. 持久化、测试与文档

- [ ] 3.1 保持现有 localStorage 与 hash 恢复结构，补齐“首次访问空工作区”“有效 hash 优先激活并加入打开标签集合”的实现与回归验证。
- [ ] 3.2 为工作区新增针对打开、切换、关闭、刷新恢复和会话重用的自动化测试。
- [ ] 3.3 更新架构文档与 tool authoring 文档，明确“切 tab 不销毁，关 tab 才销毁”以及可选生命周期接口的使用方式。